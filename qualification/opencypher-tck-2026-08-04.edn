{:receipt/type :opencypher-tck-grammar-coverage
 :receipt/at "2026-08-04"
 :receipt/tool "scripts/opencypher_tck.cljs"
 :receipt/subject "kotobase.server.cypher/parse"
 :receipt/kotobase-server-sha "3b28df2325df38794dffa94f715d9a2d1cc8c34a"

 ;; ------------------------------------------------------------------
 ;; この数字が何であって何でないか。ここを読まずに数字だけ引用しないこと。

 :receipt/measures
 "openCypher TCK の feature ファイルに現れる問い合わせクエリ文字列のうち、
  kotobase の Cypher subset パーサが受理する割合と、拒否理由の内訳。"

 :receipt/does-not-measure
 "意味論的適合。パーサを通ったクエリが TCK の期待する結果を返すかは検証して
  いない。したがってこの数を『openCypher 適合率』と呼んではならない。
  正しい呼び名は『文法カバレッジ』。"

 :receipt/why-so-low
 "TCK は Cypher エンジン全体(CREATE/MERGE/WITH/UNWIND/リスト内包/パス/ラベル/
  可変長パス/関数)を対象にする。kotobase が実装しているのは意図的に狭い
  read-only subset(MATCH の三項パターン + 等値/比較 WHERE + RETURN + 集約 +
  ORDER BY/LIMIT)で、両者の対象範囲が最初から違う。低い数字は『壊れている』
  ではなく『主張範囲が狭い』を意味する —— ただし外部から見れば
  『Cypher を喋る』という表現が過大であることも同時に意味する。"

 ;; ------------------------------------------------------------------
 ;; 実測

 :tck/upstream "https://github.com/opencypher/openCypher"
 :tck/upstream-sha "370fe27f417730dca2ef712dd1c0c5dadcb99ef8"
 :tck/feature-files 220
 :tck/query-count 1644
 :tck/setup-count 728
 :tck/setup-note
 "TCK の大半の scenario は `Given an empty graph` + `having executed: CREATE ...`
  で状態を作る。kotobase の Cypher は read-only なのでこの 728 件は構造的に
  実行できず、上の 1,644 件とは別に数えている。"

 :kotobase/parsed 1
 :kotobase/rejected 1643
 :kotobase/parse-rate 0.0006

 :kotobase/rejection-reasons
 {"must start with MATCH" 697
  "character outside the supported subset" 383
  "a bare (var) with no properties and no relationship binds nothing" 256
  "node must close with ) or contain {props}" 151
  "node needs a variable name (anonymous nodes unsupported)" 69
  "node pattern must open with (" 38
  "relationship needs [:attr]" 33
  "RETURN required" 11
  "right-to-left relationships unsupported" 3
  "only left-to-right -[:attr]-> supported" 1
  "only ORDER BY / LIMIT allowed after RETURN" 1}

 :kotobase/largest-gaps-implied
 ["ラベル `(n:Person)` 未対応 — Cypher で最も基本的なパターンが通らない"
  "暗黙の全走査 `MATCH (n) RETURN n` を意図的に拒否(設計判断であって欠陥ではない)"
  "MATCH 以外で始まる句(WITH / UNWIND / RETURN 単独 / CREATE 系)が全て対象外"
  "匿名ノード `()` 未対応"
  "可変長パス `-[:r*]->` 未対応"]

 ;; ------------------------------------------------------------------
 ;; この計測そのものが見つけた欠陥(数字より価値がある部分)

 :receipt/defects-found
 [{:defect "可変長パス `-[:r*]->` が 1 ホップ `-[:r]->` として黙って受理されていた"
   :why "tokenizer の正規表現に `*` が無く、re-seq が未知文字を黙って捨てていた"
   :impact "エラーではなく『違う質問の答え』を返す。呼び出し側から検出できない"
   :status :fixed
   :fix "kotobase-server 3b28df2 — tokenizer に catch-all を足し、正規トークンに
         full-match しない文字を loud reject する"}
  {:defect "`WHERE a.n =~ \"x\"`(正規表現マッチ)が等値比較として黙って受理されていた"
   :why "同上。`~` が捨てられていた"
   :impact "部分一致を求めた呼び出しが完全一致の結果を受け取る"
   :status :fixed
   :fix "同上"}]

 :receipt/note-on-doctrine
 "この ns の docstring は当初から「every unsupported form is rejected loudly ...
  never silently misread」と宣言していた。宣言はあったが**それを検査する
  テストが無かった**ため、2 件とも長期間気付かれなかった。再発防止として
  test/kotobase/server/cypher_test.cljc(6 deftest)を新設した。
  ADR-2607250100 の『バーの 3 条件』でいう ③(回帰を検出する仕組み)が
  欠けていた典型例。"

 :receipt/verification
 "npm run test:cljs → 88 tests / 394 assertions / 0 failures 0 errors
  (修正 + 新規テスト込み、2026-08-04)"}
